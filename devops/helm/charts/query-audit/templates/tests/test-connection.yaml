apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "query-audit.fullname" . }}-test-connection"
  labels:
    {{- include "query-audit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "query-audit.fullname" . }}:{{ .Values.service.port }}"]
  restartPolicy: Never
