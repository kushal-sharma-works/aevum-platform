apiVersion: v1
kind: Service
metadata:
  name: {{ include "aevum-ui.fullname" . }}
  labels:
    {{- include "aevum-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "aevum-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

