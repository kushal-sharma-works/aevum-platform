apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aevum-ui.fullname" . }}-test-connection"
  labels:
    {{- include "aevum-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "aevum-ui.fullname" . }}:{{ .Values.service.port }}"]
  restartPolicy: Never
