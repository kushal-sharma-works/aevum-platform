apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-timeline.fullname" . }}-test-connection"
  labels:
    {{- include "event-timeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "event-timeline.fullname" . }}:{{ .Values.service.publicPort }}"]
  restartPolicy: Never
